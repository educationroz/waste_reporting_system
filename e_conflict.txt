[1mdiff --cc templates/web_app/base.html[m
[1mindex 7581baf,4524192..0000000[m
[1m--- a/templates/web_app/base.html[m
[1m+++ b/templates/web_app/base.html[m
[36m@@@ -375,6 -357,64 +375,67 @@@[m [mdocument.addEventListener("DOMContentLo[m
          }, 4000);[m
      };[m
  [m
[32m++<<<<<<< HEAD[m
[32m++=======[m
[32m+     // Global double-submit guard for any button that fires an async CRUD call.[m
[32m+     //[m
[32m+     // Without this, a user who double-clicks "Create" sends the request twice[m
[32m+     // before the first response lands, and the server happily creates two[m
[32m+     // records. Every write action on the site funnels through here.[m
[32m+     //[m
[32m+     // Usage:[m
[32m+     //     await withButtonBusy(btn, 'Saving...', async () => { ...fetch... });[m
[32m+     //[m
[32m+     // `btn` may be null (keyboard-triggered paths, buttons rendered later) —[m
[32m+     // the work still runs, it just isn't visually locked.[m
[32m+     //[m
[32m+     // The button is restored in a `finally`, so a thrown error or an early[m
[32m+     // `return` inside the callback can never leave it stuck spinning.[m
[32m+     window.withButtonBusy = async function withButtonBusy(btn, busyText, work) {[m
[32m+         // Re-entrancy guard. `disabled` already blocks a second mouse click,[m
[32m+         // but the same handler can also be reached via Enter on a form or by[m
[32m+         // another button bound to the same function.[m
[32m+         if (btn && btn.dataset.busy === '1') return undefined;[m
[32m+ [m
[32m+         let originalHtml;[m
[32m+         if (btn) {[m
[32m+             btn.dataset.busy = '1';[m
[32m+             originalHtml = btn.innerHTML;[m
[32m+             btn.disabled = true;[m
[32m+             // textContent on a fresh span keeps busyText from being parsed as[m
[32m+             // HTML, matching the XSS posture of showToast above.[m
[32m+             const spinner = '<span class="spinner-border spinner-border-sm me-1" aria-hidden="true"></span>';[m
[32m+             btn.innerHTML = spinner + window.escapeHtml(busyText || 'Working...');[m
[32m+         }[m
[32m+ [m
[32m+         try {[m
[32m+             return await work();[m
[32m+         } finally {[m
[32m+             if (btn) {[m
[32m+                 delete btn.dataset.busy;[m
[32m+                 btn.disabled = false;[m
[32m+                 btn.innerHTML = originalHtml;[m
[32m+             }[m
[32m+         }[m
[32m+     };[m
[32m+ [m
[32m+     // Resolves the button that triggered the current inline `onclick`.[m
[32m+     // Prefer passing `this` from the markup — `onclick="doThing(5, this)"` —[m
[32m+     // and use this only as the fallback for handlers that cannot be edited.[m
[32m+     // Must be called SYNCHRONOUSLY at the top of a handler: `window.event` is[m
[32m+     // cleared once the first `await` yields control.[m
[32m+     window.eventButton = function eventButton(explicit) {[m
[32m+         if (explicit && explicit.tagName) return explicit;[m
[32m+         const ev = window.event;[m
[32m+         const target = ev && (ev.currentTarget || ev.target);[m
[32m+         return (target && target.closest) ? target.closest('button, .btn') : null;[m
[32m+     };[m
[32m+ [m
[32m+     // T1.4 FIX: Global HTML-escape helper. Use this on ANY value that came from[m
[32m+     // an untrusted source (API data, user input) before concatenating it into[m
[32m+     // an HTML string that will be assigned to innerHTML / insertAdjacentHTML.[m
[32m+     // This is the single source of truth; never inline-escape ad-hoc.[m
[32m++>>>>>>> 22e1934c3aa2112bbb8435ac5f273ab2a656eada[m
      window.escapeHtml = function escapeHtml(value) {[m
          if (value == null) return '';[m
          const s = String(value);[m
[36m@@@ -613,9 -666,15 +682,11 @@@[m
                  complaintsBadge.textContent = '';[m
              }[m
          })[m
[31m-         .catch(() => {});[m
[32m+         .catch((e) => {[m
[32m+             console.error('Failed to refresh complaints badge:', e);[m
[32m+         });[m
      }[m
  [m
[31m -    // ── Websocket connect ─────────────────────────────────────────────────[m
[31m -    // Yo <script> block <body> ko tallo tira cha, #page-content bhitra[m
[31m -    // haina, tesले htmx boost navigation ma yo pheri run hudaina —[m
[31m -    // socket eutai plain page-load ma matra khulcha, jun thikai ho.[m
      function connectNotifSocket() {[m
          try {[m
              const protocol = window.location.protocol === 'https:' ? 'wss://' : 'ws://';[m
